{{ form_start(form) }}
{{ form_errors(form) }}

{{ form_row(form.username) }}
{{ form_row(form.roles) }}
{{ form_row(form.binanceApiKey) }}
{{ form_row(form.binanceApiSecret) }}
{{ form_row(form.plainPassword) }}

<div class="mt-3">
    <button class="btn btn-primary" type="submit">Save</button>
    <a class="btn btn-danger" href="{{ path('admin_user_list') }}">Cancel</a>
</div>

{{ form_end(form) }}